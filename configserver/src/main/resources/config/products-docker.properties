spring.datasource.url=jdbc:postgresql://postgres:5432/shophouse_products
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace
logging.level.org.hibernate.type.descriptor.sql=trace
logging.level.com.products=trace

# liquibase configuration
spring.liquibase.change-log=classpath:db/changelog/db.changelog.master.xml

# multipart configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

app.properties.app-url=http://localhost:8073/uploads/

# Public URL prefix for exposing uploaded files
app.properties.files-url=http://localhost:8073/uploads/

#logging.level.org.springframework.cache=DEBUG

# API Gateway URL (configure based on environment)
app.properties.gateway-url=${API_GATEWAY_URL:http://gatewayserver:8075}

# Swagger UI configuration - use relative paths
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true

# API docs configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.api-docs.enabled=true

# Packages to scan for API documentation
springdoc.packages-to-scan=com.products.controller

# Paths to include
springdoc.paths-to-match=/**

# Swagger UI security configuration
springdoc.swagger-ui.persistAuthorization=true
springdoc.swagger-ui.displayOperationId=true

# resilience config
spring.cloud.openfeign.circuitbreaker.enabled=true
resilience4j.circuitbreaker.config.default.slidingWindowSize=10
resilience4j.circuitbreaker.config.default.permittedNumberOfCallsInHalfOpenState=10
resilience4j.circuitbreaker.config.default.failureRateThreshold=50
resilience4j.circuitbreaker.config.default.waitDurationInOpenState=10000

# retry config
#resilience4j.retry.config.default.maxRetryAttempts=3
#resilience4j.retry.config.default.waitDuration=1000
#resilience4j.retry.config.default.enableExponentialBackoff=true
#resilience4j.retry.config.default.exponentialBackoffMultiplier=2
#resilience4j.retry.config.default.ignoreExceptions=java.lang.NullPointerException

